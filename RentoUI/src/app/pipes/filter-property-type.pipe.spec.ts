import { FilterPropertyTypePipe } from './filter-property-type.pipe';

describe('FilterPropertyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPropertyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
