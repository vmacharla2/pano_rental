import { FilterMinrentPipe } from './filter-minrent.pipe';

describe('FilterMinrentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMinrentPipe();
    expect(pipe).toBeTruthy();
  });
});
